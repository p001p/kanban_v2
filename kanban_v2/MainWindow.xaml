<Window x:Class="kanban_v2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:kanban_v2"
        mc:Ignorable="d"
        Title="the FARM" Height="450" Width="800">
    <Grid>
        <Menu Margin="0,0,0,415">
            <MenuItem Header="Файл">
                <MenuItem Header="Открыть"  />
                <MenuItem Header="Сохранить" />
            </MenuItem>
        </Menu>
        <Grid>
            <Canvas x:Name="Canvas2" Background="LightGray" Margin="0,19,0,0">
                <Canvas.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Добавить ProjectBox" Click="AddProjectBox_Click" />
                        <MenuItem Header="Добавить ContractBox" Click="AddContractBox_Click"  />
                    </ContextMenu>
                </Canvas.ContextMenu>
            </Canvas>

        </Grid>
    </Grid>
</Window>
